apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{ .Values.backend.storageClass.name }}
provisioner: {{ .Values.storageClass.provisioner }}
parameters:
  server: {{ .Values.storageClass.server }}
  share: {{ .Values.storageClass.share }}